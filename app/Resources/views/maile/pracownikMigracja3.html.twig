W związku ze zmianą komórki organizacyjnej przez użytkownika o loginie <b>{{ imie_nazwisko }}</b>, <b>{{ data_dzien_rozpoczecia_pracy_w_nowym_db|date("Y-m-d") }}</b> należy odebrać dotychczasowe uprawnienia użytkownika w <b>{{ stary_db }}</b>, a następnie nadać upr. podstawowe przynależne w <b>{{ nowy_db }}</b>. W przypadku konieczności nadania dodatkowych uprawnień, administrator zasobu musi otrzymać stosowny wniosek, zaakceptowany przez przełożonego użytkownika w <b>{{ nowy_db }}</b>, złożony za pomocą AkD.
