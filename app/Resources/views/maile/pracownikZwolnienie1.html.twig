W związku z rozwiązaniem <b>{{ data_zmiany|date("Y-m-d") }}</b> stosunku pracy z użytkownikiem o loginie <b>{{ login }}</b>, konto użytkownika powinno zostać zablokowane i odebrane uprawnienia do wszystkich grup.
