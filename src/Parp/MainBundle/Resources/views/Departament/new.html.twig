{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Dodaj Biuro/Departament</h1>

    {% if not form.vars.valid %}
        <div class="alert alert-danger" role="alert">
            {{ form_errors(form.name)}}
            {{ form_errors(form.shortname)}}
        </div>
    {% endif %}

    <form class="form-horizontal" name="parp_mainbundle_departament" method="post" action="{{ path('departament_create') }}">
        <div class="form-group">
            {{ form_label(form.name) }}
            <div class="col-sm-10">
                {{ form_widget(form.name) }}
            </div>
        </div>
        <div class="form-group">
            {{ form_label(form.shortname) }}
            <div class="col-sm-10">
                {{ form_widget(form.shortname) }}
            </div>
        </div>
        <div class="form-group">

            <div class="col-sm-10">
                <button id="Zapisz" name="Zapisz" type="submit" class="btn btn-primary"><i class="fa fa-plus"></i> Dodaj Biuro/Apartament</button>

            </div>
        </div>   
        {{ form_widget(form._token) }}
    </form> 


<div class="btn-group">
    <a class="btn btn-primary" href="{{ path('departament') }}">
        <i class="fa fa-list"></i> Lista Biur i Departamentów
    </a>
</div>

{% endblock %}
