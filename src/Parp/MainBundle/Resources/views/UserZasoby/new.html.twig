{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserZasoby creation</h1>

    {{ form(form) }}

        <div class="btn-group">

<a class="btn btn-primary" href="{{ path('userzasoby') }}">
    <i class="fa fa-list"></i> Lista UserZasoby
</a>
</div>
{% endblock %}
