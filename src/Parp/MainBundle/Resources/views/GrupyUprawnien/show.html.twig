{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Usuwanie: GrupyUprawnien</h1>
    <div class="alert alert-danger">
        Czy na pewno chcesz skasować poniższy GrupyUprawnien ?
    </div>
    <table class="record_properties table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Kod</th>
                <td>{{ entity.kod }}</td>
            </tr>
            <tr>
                <th>Opis</th>
                <td>{{ entity.opis }}</td>
            </tr>
        </tbody>
    </table>

        <div class="btn-group">

    {{ form(delete_form) }}
</div>
{% endblock %}
