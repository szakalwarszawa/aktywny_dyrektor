{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Plik tworzenie</h1>

    {{ form(form) }}

        <div class="btn-group">

<a class="btn btn-primary" href="{{ returnUrl}}">
    <i class="fa fa-list"></i> Wstecz
</a>
</div>
{% endblock %}
